apiVersion: v1
kind: Secret
metadata:
  name: warm-up-bot-secrets
  annotations:
    argocd.argoproj.io/sync-wave: "-1"
type: Opaque
stringData:
  # Troque os valores antes de subir em produção.
  ENCRYPTION_KEY: change-this-to-a-random-32-char-key
  DB_PASS: change-me
  JWT_SECRET: change-this-jwt-secret
