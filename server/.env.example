# Criptografia (IMPORTANTE: Mude em produção!)
# Chave de 32 caracteres para AES-256
ENCRYPTION_KEY=change-this-to-random-32-chars!!

# MySQL
DB_HOST=localhost
DB_PORT=3306
DB_NAME=esquenta_chips
DB_USER=root
DB_PASS=

# Servidor
PORT=3001
NODE_ENV=development

# Webhook (URL publica para receber mensagens)
WEBHOOK_URL=http://localhost:3001/api/webhooks

# NOTA: APIBrasil e OpenAI agora são configurados via interface
# As credenciais são armazenadas CRIPTOGRAFADAS no banco de dados
