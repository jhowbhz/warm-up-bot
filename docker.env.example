# ─── Criptografia ───
# Chave de 32 caracteres para AES-256 (MUDE EM PRODUÇÃO!)
ENCRYPTION_KEY=change-this-to-random-32-chars!!

# ─── MySQL ───
# "mysql" é o nome do serviço no docker-compose
DB_HOST=mysql
DB_PORT=3306
DB_NAME=esquenta_chips
DB_USER=root
DB_PASS=warm_up_secret

# ─── Servidor ───
PORT=3001
NODE_ENV=production

# ─── Webhook ───
# URL pública para receber mensagens (será sobrescrita pelo Cloudflare Tunnel)
WEBHOOK_URL=http://localhost:3001/api/webhooks

# NOTA: APIBrasil e OpenAI são configurados via interface web
# As credenciais ficam CRIPTOGRAFADAS no banco de dados
